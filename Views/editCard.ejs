<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Card</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Credit Card Tracker</a>
    </div>
  </nav>
  <div class="container">
    <br>
    <div class="text-center"><h2>Edit Card</h2></div>
    <br>
    <% if (card) { %>
      <!-- Form to edit the card -->
      <form action="/editProduct/<%= card.productId %>" method="POST">
        <!-- Form fields -->
        <div class="mb-3">
          <label for="cardName" class="form-label">Card Name:</label>
          <input type="text" class="form-control" id="cardName" name="cardName" value="<%= card.cardName %>" required>
        </div>
        <div class="mb-3">
          <label for="name" class="form-label">Name:</label>
          <input type="text" class="form-control" id="name" name="name" value="<%= card.name %>" required>
        </div>
        <div class="mb-3">
          <label for="benefits" class="form-label">Benefits:</label>
          <textarea class="form-control" id="benefits" name="benefits" rows="4" required><%= card.benefits %></textarea>
        </div>
        <div class="mb-3">
          <label for="image" class="form-label">Image URL:</label>
          <input type="text" class="form-control" id="image" name="image" value="<%= card.image %>" required>
        </div>
        <button type="submit" class="btn btn-primary">Update Card</button>
      </form>
    <% } else { %>
      <p>No card found.</p>
    <% } %>
  </div>
</body>
</html>
